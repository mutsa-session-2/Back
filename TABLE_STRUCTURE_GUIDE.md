# 테이블 구조 설명

## 📋 Schedules vs Floors - Title 차이점

### 구조 비교

```
┌─────────────────────────────────────────────────┐
│ Schedule (일정/프로젝트)                          │
├─────────────────────────────────────────────────┤
│ title: "토익 900점 달성"                 ← 전체 제목 │
│ originalGoal: "3주 안에 토익 900점 달성하기"      │
│ goalSummary: "RC/LC 균형 학습..."               │
│ startDate: 2025-11-16                          │
│ endDate: 2025-12-07                            │
├─────────────────────────────────────────────────┤
│ └─ Floors (세부 계획/층) ↓                       │
│    ├─ Floor 1                                  │
│    │  title: "RC 문법 기본 복습"      ← 단계 제목 │
│    │  scheduledDate: 2025-11-16              │
│    │                                          │
│    ├─ Floor 2                                  │
│    │  title: "LC Part 1-2 집중"      ← 단계 제목 │
│    │  scheduledDate: 2025-11-17              │
│    │                                          │
│    └─ Floor 3                                  │
│       title: "실전 모의고사 1회"     ← 단계 제목 │
│       scheduledDate: 2025-11-20              │
└─────────────────────────────────────────────────┘
```

### 비유로 이해하기

| 개념 | Schedule.title | Floors.title |
|------|----------------|--------------|
| **책** | 책 제목: "해리포터와 마법사의 돌" | 챕터: "1장: 살아남은 아이", "2장: 사라진 유리" |
| **앨범** | 앨범 제목: "Love Yourself" | 곡 제목: "DNA", "MIC Drop", "Go Go" |
| **여행** | 여행 제목: "유럽 배낭여행" | 일정: "파리 에펠탑", "로마 콜로세움", "베니스 곤돌라" |
| **프로젝트** | 프로젝트: "쇼핑몰 웹사이트 개발" | 태스크: "로그인 구현", "장바구니 기능", "결제 시스템" |

### UI에서 표시 예시

```
[일정 카드]
┌──────────────────────────────────┐
│ 🎯 토익 900점 달성               │ ← Schedule.title
│ 2025-11-16 ~ 2025-12-07         │
│                                  │
│ 📝 오늘 할 일                     │
│ ☐ RC 문법 기본 복습              │ ← Floor.title
│ ☐ 단어 암기 100개                │ ← Floor.title
│                                  │
│ 📊 진행률: 3/15 완료 (20%)       │
└──────────────────────────────────┘
```

---

## 🎨 Characters - equipped_items 필드

### 데이터 구조

```json
{
  "hat": 5,        // 모자 아이템 ID
  "face": 12,      // 얼굴 아이템 ID  
  "accessory": 8,  // 악세서리 아이템 ID
  "background": 3  // 배경 아이템 ID
}
```

### Character 테이블 (수정 후)

| 필드 | 타입 | 설명 | 예시 |
|------|------|------|------|
| character_id | INT | 캐릭터 고유 ID | 1 |
| user_id | INT | 유저 ID (1:1) | 42 |
| image_url | VARCHAR(255) | S3 이미지 URL | `https://...s3.../char.png` |
| **equipped_items** | JSON | 장착 중인 아이템 | `{"hat": 5, "face": 12}` |
| created_at | TIMESTAMP | 생성일 | 2025-11-15 |

### 사용 예시

```java
// 1. 아이템 장착
character.setEquippedItems("{\"hat\": 5, \"face\": 12, \"accessory\": 8}");

// 2. Jackson으로 파싱
ObjectMapper mapper = new ObjectMapper();
Map<String, Integer> items = mapper.readValue(
    character.getEquippedItems(), 
    new TypeReference<Map<String, Integer>>() {}
);

// 3. 특정 아이템 조회
Integer hatId = items.get("hat");  // 5

// 4. 아이템 변경
items.put("hat", 10);  // 모자 교체
character.setEquippedItems(mapper.writeValueAsString(items));
```

### Items 테이블과 연계

```
User_Inventory (보유 아이템)
├─ item_id: 5  (type: "hat", name: "마법사 모자")
├─ item_id: 10 (type: "hat", name: "해적 모자")
└─ item_id: 12 (type: "face", name: "선글라스")

Character.equipped_items
└─ {"hat": 5, "face": 12}  ← 보유한 아이템 중 현재 착용 중
```

---

## 🔄 데이터 흐름

### 일정 생성 시

```
1. 사용자 입력
   ↓
   goal: "한 달 안에 Spring Boot 마스터"
   ↓
2. AI 처리
   ↓
   Schedule.title = "Spring Boot 마스터"
   Schedule.originalGoal = "한 달 안에 Spring Boot 마스터"
   Schedule.goalSummary = "JPA, Security, Batch 순서로 학습..."
   ↓
3. AI가 Floors 생성
   ↓
   Floor 1: "Spring Boot 기초 설정"
   Floor 2: "JPA 엔티티 설계"
   Floor 3: "Security JWT 구현"
   ...
```

### 캐릭터 꾸미기 시

```
1. 상점에서 아이템 구매
   ↓
   User_Inventory에 item_id: 10 추가
   ↓
2. 아이템 장착
   ↓
   기존: {"hat": 5, "face": 12}
   변경: {"hat": 10, "face": 12}  ← 모자 교체
   ↓
3. Character.equipped_items 업데이트
   ↓
4. 프론트에서 렌더링
   - Items 테이블에서 item_id: 10 조회
   - image_url 가져와서 캐릭터에 적용
```

---

## ✅ 요약

| 항목 | 설명 |
|------|------|
| **Schedule.title** | 전체 일정/프로젝트의 제목 |
| **Floor.title** | 각 세부 단계/할 일의 제목 |
| **Character.equipped_items** | 현재 장착 중인 아이템 ID (JSON) |
| **관계** | Schedule 1개 → Floors N개 (1:N) |
| **비유** | 앨범 제목 vs 곡 제목 |
